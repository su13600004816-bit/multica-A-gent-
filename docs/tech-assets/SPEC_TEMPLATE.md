<!--
强制说明书模板 v1 · 归属总任务 PL-150
用法:复制本文件到 docs/tech-assets/<技术名>/SPEC.md,逐项填空。
铁律:① 每个【】占位都要填,不填即缺项,审核打回;不适用写"不适用:原因",不许留空。
      ② 凡写源码路径,必须经 grep/打开核对、真实存在;不许凭印象。审核会逐条点开验证。
-->

# <技术名> 强制说明书(SPEC）

| 字段 | 内容 |
|---|---|
| 技术名称 | 【中文名 / 英文目录名,如 上传照片压缩 / image-compression】 |
| 归档目录 | `docs/tech-assets/【目录名】/` |
| 负责线 | 【T01 / T02 / T03】 |
| 当前状态 | 【已部署生产 / 已部署待验 / 仅代码未部署】 |
| 对应任务 | 【PL-xxx】 |
| 维护人 | 【填写 agent/小队】 |
| 最后更新 | 【YYYY-MM-DD】 |

## 1. 技术原理与作用
【这项技术解决什么问题、核心思路一句话讲清,再展开。让接手新系统的人不看代码也能懂它干嘛。】

## 2. 核心源码文件清单(真实路径)
> 必须是仓库内真实存在的路径,审核逐条点开核对。标注哪个是入口、哪个是核心逻辑。

| 文件路径 | 角色 | 说明 |
|---|---|---|
| `【apps/.../xxx.ts】` | 入口 | 【】 |
| `【apps/.../yyy.ts】` | 核心逻辑 | 【】 |
| `【...】` | 配置/类型 | 【】 |

**调用关系/数据流**:【入口 → 处理 → 输出,串一遍;关键时序/状态流转画清楚】

## 3. 对外接口 / API / 事件
> 别人(新系统)要怎么调用它。每个接口写明 入参 / 出参 / 触发时机。

| 接口/函数/事件 | 入参 | 出参 | 说明 |
|---|---|---|---|
| `【funcName()】` | 【】 | 【】 | 【】 |

## 4. 依赖项 / 环境变量 / 配置
- **第三方依赖(含版本)**:【npm 包 / 系统库 / 外部服务】
- **环境变量**:【KEY=用途,逐个列】
- **配置项 / 默认值**:【】
- **运行前置条件**:【需要的服务、端口、权限】

## 5. 迁移到新系统的步骤(本说明书核心)
> 目标是让新系统能直接拿走复用。写清解耦点。

1. **要带走的文件**:【列出迁移时必须一起搬的文件/目录】
2. **依赖当前系统的耦合点**:【它现在依赖了哪些本系统特有的东西(DB表/全局状态/某服务),这些是解耦时要切掉或替换的】
3. **需要重写的胶水代码**:【接到新系统时哪些适配层要重写】
4. **迁移步骤**:【一步步:复制 → 改依赖 → 接接口 → 配环境 → 验证】
5. **风险/注意**:【迁移时容易踩的坑】

## 6. 已知 BUG / 限制 / 坑
> 诚实写,尤其画布编排、多智能体配合相关的问题。写清现象 + 触发条件 + 影响 + 是否有临时绕法。

| 现象 | 触发条件 | 影响 | 绕法/状态 |
|---|---|---|---|
| 【】 | 【】 | 【】 | 【】 |

## 7. 验证方法
> 怎么证明它真能跑(在当前系统 + 在新系统)。给出可复现的命令/步骤,不要只写"测过了"。

- **构建门禁**:【tsc 通过 / build 通过 的命令与结果】
- **功能验证**:【具体操作步骤 + 预期结果;能附真机/浏览器截图最好】
- **新系统可用性验证**:【迁过去后用什么最小用例证明它活着】

---
<!-- 填完自检:7 节是否都填?路径是否都真实?BUG 节是否诚实?验证是否可复现?缺一审核打回。 -->
